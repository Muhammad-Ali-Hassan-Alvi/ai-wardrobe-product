/**
 * Database seed script — populate dev fixtures in Sprint 1.
 */
async function main() {
  console.log("No seed data yet — add models in Sprint 1.");
}

main()
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
